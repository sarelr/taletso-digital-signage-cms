import { getDb } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function SettingsPage(){ const [roles,sites,screens]=await Promise.all([getDb().role.findMany({include:{permissions:{include:{permission:true}}},orderBy:{name:"asc"}}),getDb().location.count(),getDb().screen.count()]); return <div className="p-5 lg:p-8"><h1 className="text-3xl font-bold text-[#07172c]">Platform Settings</h1><p className="mt-2 text-slate-500">Current local deployment and access-control configuration.</p><div className="mt-7 grid gap-4 md:grid-cols-3"><div className="card p-5"><div className="text-sm text-slate-500">Registered screens</div><div className="mt-2 text-3xl font-bold">{screens}</div></div><div className="card p-5"><div className="text-sm text-slate-500">Sites</div><div className="mt-2 text-3xl font-bold">{sites}</div></div><div className="card p-5"><div className="text-sm text-slate-500">Player architecture</div><div className="mt-2 font-bold">Web + future native</div></div></div><section className="card mt-6 p-5"><h2 className="text-lg font-bold">Roles and permissions</h2><div className="mt-4 space-y-4">{roles.map(role=><div key={role.id}><div className="font-semibold">{role.name}</div><div className="mt-1 flex flex-wrap gap-2">{role.permissions.map(({permission})=><span key={permission.id} className="rounded-full bg-slate-100 px-3 py-1 text-xs">{permission.key}</span>)}</div></div>)}</div></section><div className="mt-5 rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900">Network addresses and secrets remain environment-managed. This page intentionally does not display credentials or make infrastructure changes.</div></div>; }

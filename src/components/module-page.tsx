import { Download, Plus } from "lucide-react";
import { moduleData } from "@/lib/mock-data";
export function ModulePage({ module }: { module: keyof typeof moduleData }) {
  const data = moduleData[module];
  return <div className="p-5 lg:p-8"><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-bold text-[#07172c]">{data.title}</h1><p className="mt-2 max-w-2xl text-slate-500">{data.description}</p></div><div className="flex gap-2"><button className="flex items-center gap-2 rounded-xl border bg-white px-4 py-2.5 text-sm font-semibold"><Download size={17}/>Export</button><button className="flex items-center gap-2 rounded-xl bg-[#07172c] px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>New record</button></div></div>
    <div className="card overflow-hidden"><div className="flex flex-wrap justify-between gap-3 border-b p-4"><input className="min-w-64 rounded-lg border px-3 py-2" aria-label={`Search ${data.title}`} placeholder="Search records..."/><select className="rounded-lg border px-3 py-2" aria-label="Filter records"><option>All campuses</option><option>Mahikeng</option><option>Lichtenburg</option><option>Lehurutshe</option><option>Central Offices</option></select></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{data.columns.map((column) => <th key={column} className="px-5 py-4">{column}</th>)}</tr></thead><tbody>{data.rows.map((row) => <tr key={row.join("-")} className="border-t border-slate-100">{row.map((cell,index) => <td key={cell} className={`px-5 py-4 ${index === 0 ? "font-semibold text-[#07172c]" : "text-slate-600"}`}>{cell}</td>)}</tr>)}</tbody></table></div></div>
  </div>;
}
